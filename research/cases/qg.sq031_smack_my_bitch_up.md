# qg.sq031_smack_my_bitch_up — A Cool Metal Fire

## 当前判断

- 状态：`rejected`
- 原始种子：QuestGuide 将其放进 Johnny / ending 相关集合。

## 为什么拒绝独立提示

PowerPyx walkthrough 明确说明：这是 `Chippin' In` 中自动发生的一段 interlude，玩家可以随意互动，但结果始终相同。

因此：

- 它本身不是一个独立的关键选择节点；
- 不应该显示 quest-level `影响：...`；
- 不应该给内部 objective 加 `[值得留意]` / `[重要阶段]`；
- 真正有后果的选择应归到 `Chippin' In` 油田墓地对话。

## 研究价值

保留这条 rejected 记录很重要：它证明 QuestGuide 的 ending 分类是“任务链相关性”而不是“每个任务内部都有决定性选择”，因此我们的系统必须继续做 objective-level 二次筛选。
